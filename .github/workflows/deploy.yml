name: Check

on:
  push:
    branches:
    - main

jobs:
  ui:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Print Message
      run: echo "Hello, GitHub Actions!"
      env:
        run: echo ${{ secrets.AWS_ACCESS_KEY }}
